[sncast.default]
# Blast API was retired; this endpoint is verified working and supports the full
# method set (getClassAt included, which some public endpoints omit).
url = "https://api.cartridge.gg/x/starknet/sepolia"
